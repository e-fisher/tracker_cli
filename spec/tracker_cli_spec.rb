require 'spec_helper'

describe TrackerCli do
  it 'has a version number' do
    expect(TrackerCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
